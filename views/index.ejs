<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>Tokopaedi</title>
  </head>
  <body>
    <div class="cards">
      <% DataProduk.forEach (i => { %>
      <div class="card">
        <img src="<%= i.gambar_produk %>" width="200" height="150" />
        <h1><%= i.nama_produk %></h1>
        <p class="deskripsi-produk"><%= i.deskripsi_produk %></p>
        <p><b>Rp.<%= i.harga_produk %></b></p>
        <button><a href="/produk/<%= i.id %>"> Detail Produk</a></button>
      </div>
      <% }) %>
    </div>
  </body>
</html>
